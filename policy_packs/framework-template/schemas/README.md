# Custom Policy Pack Schemas

Copy the schema files from `policy_packs/generic-ssdlc/schemas/` and replace
the field/control definitions with your organization-specific control library.

Keep restricted controls, non-public prompt text, and organization-specific
brand assets out of the public repository.
